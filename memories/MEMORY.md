When working with Google Maps API for lead generation, use batch processing with background processes to avoid timeouts. Each batch should cover 20-50 cities with 4 keywords (roofing, roof repair, roof contractor, roofing company). Store API key in files rather than inline to avoid syntax issues. Use 50km radius per city, 2 pages max per keyword, 0.2s delay between requests to avoid rate limits.